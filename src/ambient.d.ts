type BallColor = "bg-union-purple" | "bg-whatsapp-green" | "bg-black" | "instagram"
type BallInfo = {
    symbol: string,
    description: string,
    ballColor: BallColor,
    linkTo: string,
    delay: number
}